#include "main.h"

/**
 * rev_string - thsi function will reverse a string
 * @s: pointer to the string
 * Return: return void
 */

void rev_string(char *s)
{
	int i, L = 0;
	char r[10];

	while (s[L] != '\0')
		L++;

	for (i = 0; s[i] != '\0'; i++)
		r[L - i] = s[i];
	*s = *r;
}
