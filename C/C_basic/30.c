#include <stdio.h>

int main()
{
    int N;
    printf("Input a number(Greater than 1): ");
    scanf("%i", &N);

    for (int i = 1; i < N; i++)
    {
        if ((i % 2) == 0)
        {
            printf("%i^%i = %i", );
        }
    }
}